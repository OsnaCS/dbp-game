<%= form_for(@expedition_instance) do |f| %>
  <% if @expedition_instance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@expedition_instance.errors.count, "error") %> prohibited this expedition_instance from being saved:</h2>

      <ul>
      <% @expedition_instance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :expidition_id %><br>
    <%= f.number_field :expidition_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
