<p id="notice"><%= notice %></p>

<h1>Einheiten</h1>

<table id="table_units">
  <thead>
    <tr>
      <th>Bild</th>
      <th>Name</th>
      <th>Metall</th>
      <th>Kristall</th>
      <th>Treibstoff</th>
      <th>Vorhanden</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @units.each do |unit| %>
      <tr>
        <td></td>
        <td><%= unit.name %></td>
        <td><%= unit.metal_price %></td>
        <td><%= unit.crystal_price %></td>
        <td><%= unit.fuel_price %></td>
        <td></td>
        <td>
          <%= form_tag build_unit_path(unit), method: 'get' do %>
          <%= number_field_tag(:amount, :unit_count, placeholder: '0', min: 0,) %>
            <td> <%= submit_tag 'Herstellen' %> </td>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>