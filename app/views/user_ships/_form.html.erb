<%= form_for(@user_ship) do |f| %>
  <% if @user_ship.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_ship.errors.count, "error") %> prohibited this user_ship from being saved:</h2>

      <ul>
      <% @user_ship.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :ship_id %><br>
    <%= f.number_field :ship_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
