<%= form_for(@build_list) do |f| %>
  <% if @build_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@build_list.errors.count, "error") %> prohibited this build_list from being saved:</h2>

      <ul>
      <% @build_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
